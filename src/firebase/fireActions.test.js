import { addResume } from "./fireActions"

//create a test for fireActions
test("publicar un objeto a la db", () => {
    //addResume("resumen", "autores", "titulo", "fecha", "categorias", "pagina")
    expect(2).toBe(2)
})